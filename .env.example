# Copy this file to `.env.local` (for Vite dev) or export the variables in your CI/CD.

# Supabase project URL (https://<project-id>.supabase.co)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Public anon key (found in Supabase dashboard → Settings → API)
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Optional: custom edge function path
VITE_SUPABASE_FUNCTION_PATH=/functions/v1/make-server-2fc91c13
